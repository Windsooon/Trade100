# =============================================================================
# PROXY CONFIGURATION (for regions where Polymarket API is not accessible)
# =============================================================================
# Uncomment ONE of the following lines if you need proxy access:

# Option 1: HTTP proxy (most common)
# ALL_PROXY=http://127.0.0.1:9988

# Option 2: SOCKS5 proxy
# ALL_PROXY=socks5://127.0.0.1:1080

# Option 3: Standard HTTP proxy environment variables
# HTTP_PROXY=http://127.0.0.1:9988
# HTTPS_PROXY=http://127.0.0.1:9988

# Note: The app automatically detects proxy configuration and uses it when available.
# No proxy is needed for Vercel deployments or regions with direct API access.

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Polymarket API URLs (defaults, usually don't need to change)
NEXT_PUBLIC_POLYMARKET_API_URL=https://gamma-api.polymarket.com
NEXT_PUBLIC_POLYMARKET_WS_URL=wss://ws-subscriptions-clob.polymarket.com

# =============================================================================
# OPTIONAL FEATURES
# =============================================================================
# Trading Configuration (optional)
POLYMARKET_PRIVATE_KEY=your_ethereum_private_key_for_trading

# Cron Secret for market sync (optional)
CRON_SECRET=your_secret_for_cron_endpoint 